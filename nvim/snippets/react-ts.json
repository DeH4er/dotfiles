{
  "export default component": {
    "prefix": "c",
    "body": [
      "export default function $TM_FILENAME_BASE() {",
      "  return (",
      "    $1",
      "  )",
      "}"
    ]
  },
  "Export default component with interface": {
    "prefix": "ci",
    "body": [
      "export interface ${TM_FILENAME_BASE}Props {",
      "}",
      "",
      "export default function $TM_FILENAME_BASE({}: ${TM_FILENAME_BASE}Props) {",
      "  return (",
      "    $1",
      "  )",
      "}"
    ]
  },
  "Export default component with interface and children": {
    "prefix": "cic",
    "body": [
      "export interface ${TM_FILENAME_BASE}Props extends React.PropsWithChildren {",
      "}",
      "",
      "export default function $TM_FILENAME_BASE({children}: ${TM_FILENAME_BASE}Props) {",
      "  return (",
      "    $1",
      "  )",
      "}"
    ]
  }
}
